# 分阶段优化计划与执行记录（2026-03-06）

## 目标

- 完成 P0 阶段落地并形成可复验门禁
- 定义 P1/P2 的执行路径、优先级和验收口径
- 确保性能与用户体验优化可持续推进

## P0（第1周）执行结果

### 1) 修复 performance_test.py 阻塞缺陷

- 已修复缓存测试口径错误：容量与断言一致，验证命中结果可复现
- 已修复事件发布调用错误：改为发布 `MessageDeltaEvent` 实例
- 已修复事件接收准确性：队列容量与发送量一致，接收数与发送数一致断言通过
- 验证结果：`python3 benchmarks/performance_test.py` 退出码 0，三项性能子测试全部通过

### 2) 补齐核心业务冒烟测试并支持每日触发

- 新增核心冒烟用例文件：`tests/integration/test_core_business_smoke.py`
- 覆盖主干路径：
  - 用户登录路径：认证头注入与鉴权参数生效
  - 关键交易路径：交易请求记录、过滤与导出
  - 数据落库路径：会话与消息持久化写入/读取
- 新增 pytest 标记：`smoke`
- 新增每日自动触发工作流：`.github/workflows/daily-smoke.yml`
- 验证结果：`pytest tests/integration/test_core_business_smoke.py -q -m smoke` 全部通过

### 3) 集成性能一致性校验到 CI（门禁/报告/告警）

- 升级性能校验脚本：`scripts/check_benchmark_thresholds.py`
  - 支持输出 JSON/Markdown 报告
  - 输出路径可配置，便于流水线复用
- 升级 CI：`.github/workflows/ci.yml`
  - 增加 `smoke` 作业，作为主测试前置门禁
  - `performance-gate` 生成并上传可视化报告工件
  - 将性能报告写入 Job Summary
  - 增加 Slack/飞书失败告警（Webhook Secret 可选）
- 结果样例：`benchmarks/results/performance-gate-report.md` 当前状态为 PASS

## 当前验收状态（对齐你定义的标准）

- 功能回归 0 阻断失败：本轮新增/修复项回归通过，核心冒烟 3/3 通过
- 性能不低于基线 95%：门禁阈值已切换至 baseline 95%，最新校验为 PASS
- 关键路径体验持续提升：已建立 P1/P2 指标采集与周度评审机制，进入执行排期

## P1（第2-3周）执行计划

### 优先级

- P1-1 高优：TUI 渲染与事件分发热点定位与优化
- P1-2 高优：会话/存储批处理重构与延迟收敛
- P1-3 中高优：性能回归看板与劣化预警自动指派

### 落地项

- 采样与定位
  - 接入 `py-spy`/`cProfile` 生成火焰图并固化热点清单
  - 对主线程阻塞段建立 TopN 追踪
- 性能优化
  - 优化事件分发热点路径，削减重复分配与高频序列化
  - 引入异步批量写入策略与请求合并，收敛 P95 时延
  - 保持内存上限约束，避免以空间换时间导致峰值失控
- 可观测性
  - 新增每日性能对比任务，自动产出 CPU/内存/I/O/网络指标对比
  - 劣化 >5% 触发告警并附带责任人字段

### P1 验收标准

- 主线程阻塞耗时下降 ≥30%
- P95 时延收敛至当前基线 95% 以内
- 内存占用峰值不高于优化前

## P2（第4周）执行计划

### 优先级

- P2-1 高优：错误引导与恢复提示体系
- P2-2 高优：主题预览可访问性增强
- P2-3 高优：高风险操作防误触强化

### 落地项

- 引导文案
  - 按错误码与场景输出可执行下一步提示
  - 多语言文案统一校对并建立回归清单
- 主题预览
  - 对比度、字号、间距统一
  - 增加色弱友好主题
  - 以 WCAG 2.1 AA 作为验收约束
- 高风险操作
  - 删除/批量变更/交易类指令增加二次确认、倒计时、不可逆提示
  - 结合可用性测试验证误操作率下降目标

### P2 验收标准

- 用户错误恢复时长缩短 ≥50%
- 可读性评分 ≥90（WCAG 2.1 AA）
- 误操作率下降 ≥70%

## 周节奏与时间安排

- 第1周：完成 P0 收口与 95% 阈值切换（已完成）
- 第2周：完成热点定位和第一批性能优化提交
- 第3周：完成批处理重构与性能看板自动化
- 第4周：完成 UX/可访问性/高风险交互优化与可用性验证

## 本阶段风险与处置

- 风险：性能抖动导致误告警
  - 处置：引入滑动窗口与重试采样，降低偶发噪声
- 风险：优化引入回归
  - 处置：smoke + 全量回归双门禁，缺陷 24h 修复闭环
- 风险：告警渠道配置缺失
  - 处置：Slack/飞书 Webhook 以 Secret 注入，未配置时保持门禁失败阻断
